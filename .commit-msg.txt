fix(byteplus): cover remaining provider-name registrations and doc indexes

Follow-up sweep after the initial driver + docs commits to catch the
spots a global grep for `volcengine` flagged:

- `kernel/manifest_helpers.rs`: add `byteplus` and `byteplus_coding` to
  the `provider/model` prefix-recognition allowlist so model strings
  like `byteplus/seed-2-0-pro-260328` get parsed as a provider hint
  instead of falling through to the default lookup.
- `docs/.../providers/page.mdx` (en + zh): add BytePlus ModelArk to the
  Platforms section enumeration AND to the capability matrix table so
  the provider index reflects the new option.
- `docs/.../providers/platforms/page.mdx` (en + zh): add BytePlus
  ModelArk to the page's quick-nav list at the top.

No changes needed for: dashboard (loads providers dynamically from
`/api/providers`), `web/public/registry.json` (regenerated by registry
sync after librefang-registry PR #78 merges), `media/openai.rs` (only
mentions volcengine in inline doc-comments as an example, no behavior).
