# LibreFang deploy worker — auto-deployed via GitHub Actions
name = "librefang-deploy"
main = "src/index.js"
compatibility_date = "2024-12-01"
workers_dev = false

routes = [
  { pattern = "deploy.librefang.ai/*", zone_name = "librefang.ai" }
]
