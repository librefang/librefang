name: Nix Build

# Catches NixOS build breakage on flake.nix or workspace changes before
# they reach main. Past incidents (#2937, #2974, #3052, #3156, #3197) all
# slipped through because regular CI doesn't exercise the Nix path.
on:
  push:
    branches: [main]
    paths:
      - 'flake.nix'
      - 'flake.lock'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'crates/**/Cargo.toml'
      - '.github/workflows/nix-build.yml'
  pull_request:
    branches: [main]
    paths:
      - 'flake.nix'
      - 'flake.lock'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'crates/**/Cargo.toml'
      - '.github/workflows/nix-build.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  nix-build:
    name: nix build (${{ matrix.package }})
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        package:
          - librefang-cli
          - librefang-desktop
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install Nix
        uses: cachix/install-nix-action@526bbe688ef5cba464b9e39f5f5b9c35c16e7ce9 # v31.4.0
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Cache /nix/store
        uses: nix-community/cache-nix-action@135667ec418502fa5a3598af6fb9eb733a374cbf # v7.0.0
        with:
          primary-key: nix-${{ runner.os }}-${{ matrix.package }}-${{ hashFiles('flake.lock', 'Cargo.lock') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-${{ matrix.package }}-
          gc-max-store-size-linux: 5G
          purge: true
          purge-prefixes: nix-${{ runner.os }}-${{ matrix.package }}-
          purge-created: 0
          purge-last-accessed: 0
          purge-primary-key: never

      - name: Build .#${{ matrix.package }}
        run: nix build --print-build-logs .#${{ matrix.package }}
