Expose the live step progress of a workflow run — `current_step_index` and `total_steps` on the run plus each step's resolved `variables` snapshot — through `GET /api/workflows/runs/:id` and `GET /api/workflows/:id/runs`, persist `total_steps` in SQLite (schema v56) so a run recovered after a daemon restart reports real progress instead of "step X of 0", and clear the live index on every terminal transition (#8177) (@DaBlitzStein)