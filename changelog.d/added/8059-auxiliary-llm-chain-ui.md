Auxiliary LLM chain editor on the dashboard Config page (and an Auxiliary sub-tab in the TUI Settings screen): operators can view and edit per-task model chains for all nine `AuxTask` side-tasks, with chain entries suggested from the live provider/model catalog, and the task list served by the kernel on `GET /api/config/schema` (`x-aux-tasks`) so a newly added side-task appears without a UI change (#8059) (@DaBlitzStein)
