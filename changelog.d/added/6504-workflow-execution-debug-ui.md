Add a workflow execution debug console to the dashboard: a live step-progress indicator (`current_step_index`/`total_steps`, polled while a run is in flight), a per-step execution timeline showing the `{{var}}` → resolved-value bindings each step saw, and a one-click re-run control that pre-fills the run form from a previous run's stored input instead of resubmitting blind.
  Runs reloaded from SQLite after a daemon restart now recompute `total_steps` from the still-registered workflow definition instead of permanently reporting 0, and both the run-detail and list-runs endpoints expose the new fields end-to-end (#6504) (@DaBlitzStein)
