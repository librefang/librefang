The model settings modal now wires the `context_window` and `max_output_tokens` capacity overrides through to `ModelOverrides` (both were declared in the catalog and the API types but unreachable from the UI, the slider was hardcoded to `enabled={false}`), and the slider's OFF track switched from the divider hairline token to a control-weight token so enabled and disabled states read apart in dark themes.
A model whose catalog entry reports an unknown context window — which arrives on the wire as `0`, not as an absent field — now falls back to the default instead of pinning the slider to its floor and, worse, saving that `0` back as if the operator had deliberately chosen it (#8106) (@DaBlitzStein)
