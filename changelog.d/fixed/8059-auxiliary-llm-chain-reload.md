Auxiliary LLM chain edits (`[llm.auxiliary]`) now take effect on `POST /api/config/reload` instead of silently requiring a daemon restart: the section was classified restart-required while `AuxClient` is rebuilt unconditionally on config swap, so the reload plan carried neither a hot action nor a no-op change and the new config was discarded — the operator's chain edit did nothing until restart (#8059) (@DaBlitzStein)
