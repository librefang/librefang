A marketplace skill whose own `SKILL.md` frontmatter fails to parse now answers `400`, not the handler's fallback status. Install returned a `500` whose body is scrubbed, so the message naming the broken file and its syntax error was replaced with "Internal server error"; detail returned `404`, which said the skill does not exist when it exists and is unusable. (#7753) (@DaBlitzStein)
