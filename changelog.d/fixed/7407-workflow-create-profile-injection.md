`workflow_create` is now injected into every agent's tool set, mirroring the `skill_evolve_*` gate: named-profile agents (research, coding, messaging, automation, minimal) previously answered "Unknown tool" on `tool_load` because no profile lists `workflow_*` tools and `ALWAYS_NATIVE_TOOLS` only controls lazy-load schema shipping, not inclusion. Per-agent `tool_blocklist` and non-empty `tool_allowlist` filters still apply after the injection, so operators can opt agents out. (#7407) (@DaBlitzStein)
