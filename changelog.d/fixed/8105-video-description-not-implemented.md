Stop `describe_video` answering success with a placeholder string.
Both branches returned `Ok` with text like `[Video description would be generated by Gemini]`, and `MediaUnderstanding.description` is fed to an agent as what the media contained — so the model received that sentence as a description of the video and reasoned from it.
Neither path has a real implementation, so both now return `Err`; `process_attachments` already collects a per-attachment `Result`, so nothing depended on the fabricated success.
A misconfigured `[media.custom_video]` still reports the configuration error rather than the generic not-implemented one, because the config plumbing #8040 added is real even though the dispatch is not.
Also restores the Polish diacritics on the four media description strings #8040 added (`domyslnie` → `domyślnie`, `uzywana` → `używana`, and so on), which had been ASCII-folded unlike the rest of `pl.json`. (#8105) (@houko)
