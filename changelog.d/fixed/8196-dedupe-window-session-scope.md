Widen the duplicate-detection window when a memory write is session-scoped, so an agent stops storing a second copy of what the current session already told it.
The candidate list was widened from 5 to 20 rows only for an active `chat_scope`, while the session filter added by #7605 prunes that same list — and `chat_scope` is absent for every non-channel caller (dashboard, REST, CLI), which is exactly where session scoping is used.
Five rows from other sessions were enough to fill the window and be discarded, leaving the extractor to decide against an empty candidate list: a restatement was stored again, and a correction landed beside the stale fact instead of replacing it, because the update and conflict-detection paths are reachable only through a candidate (#8196) (@houko)
