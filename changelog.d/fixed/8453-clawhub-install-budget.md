`POST /api/clawhub/install` and its China-mirror twin shared the 8 s read budget the ClawHub GET routes use to stay under the route smoke test's own bound, so an install that was merely slow — a download, an extraction, a security scan — was cancelled mid-flight and answered as an unavailable marketplace.
Installs now run under their own, much longer budget, and are staged beside `skills/` and moved into place with one `rename` only once complete, so a budget expiry or a hung-up caller answers without side effects: no partial skill directory and no staging residue (#8453) (@DaBlitzStein)
