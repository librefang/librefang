A gateway-discovered model whose endpoint declares no price is no longer billed at $0: it is metered from the conservative default rates ($1/M input, $3/M output) and the dashboard shows its pricing as unavailable. A gateway that explicitly declares `0.0/0.0` keeps costing nothing.
(#8369) (@DaBlitzStein)
