Land a first visit to the Workflows page on "My Workflows" rather than bouncing it to the template library.
The empty state offers "Create your first workflow" and "Ask an agent" first, which is the discoverability surface for agent-authored workflows; the template library remains one tab click away. (#6943) (@DaBlitzStein)
