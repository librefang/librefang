apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: librefang-backups
  namespace: librefang
  labels:
    app: librefang
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
