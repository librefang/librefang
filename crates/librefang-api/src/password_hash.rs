//! Argon2id password hashing for dashboard authentication.
//!
//! Replaces the previous plaintext password comparison with Argon2id,
//! which is resistant to GPU/ASIC attacks and rainbow tables.
//!
//! Supports transparent migration from legacy plaintext passwords:
//! - If `dashboard_pass_hash` is set (Argon2id PHC string), verify against it.
//! - If only `dashboard_pass` is set (plaintext/vault), fall back to constant-time
//!   plaintext comparison and return the Argon2id hash for transparent upgrade.
//!
//! Session tokens are now generated randomly with expiration support,
//! replacing the old deterministic HMAC-SHA256-derived tokens that could
//! not be revoked or expired.

use argon2::{
    password_hash::{
        rand_core::{OsRng, RngCore},
        PasswordHash, PasswordHasher, PasswordVerifier, SaltString,
    },
    Algorithm, Argon2, Params, Version,
};
use serde::{Deserialize, Serialize};
use sha2::{Digest, Sha256};
use std::time::{SystemTime, UNIX_EPOCH};

/// Custom PHC-style prefix for high-entropy device bearers. Argon2 is built
/// for low-entropy passwords; a 256-bit CSPRNG token doesn't need the
/// salting / KDF cost, and Argon2 verify (~50–100ms) sits on the auth hot
/// path for every mobile request. SHA-256 is unsalted by design here —
/// the token is generated by the daemon with full 256-bit entropy, so a
/// rainbow table would have to enumerate 2^256 possibilities.
const SHA256_PREFIX: &str = "$sha256$";

/// Hash a password with Argon2id using recommended parameters.
///
/// Returns a PHC-format string like `$argon2id$v=19$m=19456,t=2,p=1$<salt>$<hash>`.
pub fn hash_password(password: &str) -> Result<String, argon2::password_hash::Error> {
    let salt = SaltString::generate(&mut OsRng);
    let params = Params::new(19_456, 2, 1, None)?;
    let argon2 = Argon2::new(Algorithm::Argon2id, Version::V0x13, params);
    let hash = argon2.hash_password(password.as_bytes(), &salt)?;
    Ok(hash.to_string())
}

/// Hash a high-entropy device bearer with unsalted SHA-256.
///
/// Used for mobile-pairing per-device tokens, which are 256-bit CSPRNG
/// outputs. Returns `$sha256$<hex>`; `verify_password` recognises the
/// prefix and dispatches to the SHA-256 path. Argon2's salting / KDF
/// cost is unnecessary at that entropy level and would otherwise tax
/// every authenticated mobile request (~50–100ms verify).
pub fn hash_device_token(token: &str) -> String {
    let mut hasher = Sha256::new();
    hasher.update(token.as_bytes());
    let digest = hasher.finalize();
    format!("{SHA256_PREFIX}{}", hex::encode(digest))
}

/// Mint a fresh 256-bit bearer token, hex-encoded.
///
/// The entropy source for every credential this module hashes with [`hash_device_token`]: paired-device bearers and, since #6613, a master `api_key` generated by `librefang hash-api-key --generate`.
/// 32 bytes of OS CSPRNG output is what makes the unsalted SHA-256 verifier sound — a rainbow table would have to enumerate 2^256 possibilities — so the generator and the hash belong together, where a future reader cannot lower one without seeing the other.
pub fn generate_bearer_token() -> String {
    let mut bytes = [0u8; 32];
    OsRng.fill_bytes(&mut bytes);
    hex::encode(bytes)
}

/// Is verifying against `hash_str` cheap enough to run inline on an async worker?
///
/// `true` for the `$sha256$` form (one SHA-256 over ~32 bytes, sub-microsecond) and `false` for an Argon2id PHC string, whose whole design goal is to cost ~50–100 ms of CPU.
/// Callers on a request hot path use this to decide between verifying inline and moving the work to `tokio::task::spawn_blocking`: the async runtime must never be stalled for the length of a KDF, and paying the `spawn_blocking` hop for a hash that verifies in a microsecond is pure overhead.
///
/// Deliberately keyed on the stored hash rather than on a caller-supplied flag, so the decision cannot drift from what `verify_password` will actually do with the same string.
/// An unparseable hash lands on the expensive branch, which is the safe default — `verify_password` returns `false` for it either way, and the cost is bounded by one failed `PasswordHash::new`.
pub fn is_cheap_to_verify(hash_str: &str) -> bool {
    hash_str.starts_with(SHA256_PREFIX)
}

/// Verify a password against either an Argon2id PHC string (regular user
/// keys) or our `$sha256$` prefix (device bearers). Format detection is
/// driven by the prefix on `hash_str` so a single `user_api_keys` table
/// can hold both kinds without callers having to know which is which.
pub fn verify_password(password: &str, hash_str: &str) -> bool {
    if let Some(hex_digest) = hash_str.strip_prefix(SHA256_PREFIX) {
        return verify_sha256_token(password, hex_digest);
    }
    let Ok(parsed) = PasswordHash::new(hash_str) else {
        return false;
    };
    // Use default Argon2id verifier — it reads params from the PHC string.
    Argon2::default()
        .verify_password(password.as_bytes(), &parsed)
        .is_ok()
}

/// Constant-time SHA-256 token verification. Decoding the stored hex
/// once per check is cheap (32 bytes) and `subtle::ConstantTimeEq`
/// avoids leaking match progress through timing.
fn verify_sha256_token(token: &str, expected_hex: &str) -> bool {
    use subtle::ConstantTimeEq;
    let Ok(expected) = hex::decode(expected_hex) else {
        return false;
    };
    let mut hasher = Sha256::new();
    hasher.update(token.as_bytes());
    let actual = hasher.finalize();
    actual.as_slice().ct_eq(&expected).into()
}

/// A session token with creation metadata for expiration support.
///
/// `user_name` / `user_role` are populated when the session was issued by
/// a credential flow that carries identity (e.g. `dashboard_login`), so
/// the auth middleware can build an `AuthenticatedApiUser` extension for
/// RBAC-gated handlers (`audit/query`, per-user budget writes). Both are
/// `#[serde(default)]` for backward-compat with sessions persisted to
/// disk before attribution was added — those load with `None` and keep
/// the legacy "session matches → trusted-anonymous" behaviour.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SessionToken {
    /// The hex-encoded random token string.
    pub token: String,
    /// Unix timestamp (seconds) when this token was created.
    pub created_at: u64,
    /// Display name of the principal this session was issued to (e.g.
    /// the `dashboard_user` value, or a `[[users]] name` for future
    /// API-key-driven session flows).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Role string (`"owner" | "admin" | "user" | "viewer"`) parsed by
    /// `UserRole::from_str_role` at consume time. Stored as a string to
    /// keep this crate independent of `librefang-kernel`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_role: Option<String>,
}

/// Default session TTL: 30 days.
pub const DEFAULT_SESSION_TTL_SECS: u64 = 30 * 24 * 3600;

/// Generate a cryptographically random session token.
///
/// Uses OS-level CSPRNG to produce a 32-byte (256-bit) random token,
/// paired with a creation timestamp for expiration checks.
pub fn generate_session_token() -> SessionToken {
    let mut bytes = [0u8; 32];
    OsRng.fill_bytes(&mut bytes);
    let token = bytes.iter().map(|b| format!("{b:02x}")).collect();
    let created_at = SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .unwrap_or_default()
        .as_secs();
    SessionToken {
        token,
        created_at,
        user_name: None,
        user_role: None,
    }
}

/// Check if a session token has expired based on its creation time and TTL.
pub fn is_token_expired(token: &SessionToken, ttl_secs: u64) -> bool {
    let now = SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .unwrap_or_default()
        .as_secs();
    now.saturating_sub(token.created_at) >= ttl_secs
}

/// Result of a dashboard password verification.
pub enum VerifyResult {
    /// Password matched (Argon2id or legacy). Contains the session token.
    Ok {
        token: SessionToken,
        /// If Some, the caller should persist this Argon2id hash to upgrade
        /// from the legacy plaintext password.
        upgrade_hash: Option<String>,
    },
    /// Password did not match.
    Denied,
}

fn verify_legacy_password(input_pass: &str, cfg_pass: &str) -> (bool, Option<String>) {
    use subtle::ConstantTimeEq;

    // Match the Argon2-class work performed by the preferred hash branch even
    // when the plaintext password is wrong. On success this is also the
    // migration hash, so a legacy login still pays for Argon2 only once.
    let upgrade_hash = hash_password(input_pass).ok();
    let matches = input_pass.as_bytes().ct_eq(cfg_pass.as_bytes()).into();
    (matches, upgrade_hash)
}

/// Pick the server-side secret used to derive dashboard session tokens.
fn dashboard_session_secret<'a>(cfg_pass: &'a str, pass_hash: &'a str) -> Option<&'a str> {
    if !pass_hash.is_empty() {
        Some(pass_hash)
    } else if !cfg_pass.is_empty() {
        Some(cfg_pass)
    } else {
        None
    }
}

/// Derive the dashboard session token from the configured credentials.
#[deprecated(note = "Use generate_session_token() for random tokens with expiration")]
pub fn derive_dashboard_session_token(
    username: &str,
    cfg_pass: &str,
    pass_hash: &str,
) -> Option<String> {
    let secret = dashboard_session_secret(cfg_pass, pass_hash)?;
    if username.is_empty() {
        return None;
    }
    #[allow(deprecated)]
    Some(derive_session_token(username, secret))
}

/// Verify dashboard credentials with Argon2id (preferred) or legacy plaintext fallback.
///
/// - If `pass_hash` is non-empty, verify with Argon2id only.
/// - Otherwise, fall back to constant-time plaintext comparison against `cfg_pass`.
///   On success, returns an `upgrade_hash` so the caller can transparently migrate.
///
/// **Timing safety**: Every credential mode performs Argon2-class work regardless
/// of whether the username or password matched. This prevents username enumeration
/// and avoids exposing legacy plaintext configuration through an instant branch.
///
/// Returns a randomly generated `SessionToken` on success instead of a
/// deterministic HMAC-derived token, preventing token replay and enabling
/// expiration-based revocation.
pub fn verify_dashboard_password(
    input_user: &str,
    input_pass: &str,
    cfg_user: &str,
    cfg_pass: &str,
    pass_hash: &str,
) -> VerifyResult {
    // Username is always compared with constant-time comparison.
    use subtle::ConstantTimeEq;
    let user_ok: bool = input_user.as_bytes().ct_eq(cfg_user.as_bytes()).into();

    // Always verify password to prevent timing side-channel on username enumeration.
    // Even if username is wrong, we still run Argon2id so timing is constant.
    let (pass_ok, legacy_upgrade_hash) = if !pass_hash.is_empty() {
        (verify_password(input_pass, pass_hash), None)
    } else if !cfg_pass.is_empty() {
        verify_legacy_password(input_pass, cfg_pass)
    } else {
        // No credentials configured — run a dummy hash to keep timing constant.
        let _ = hash_password(input_pass);
        (false, None)
    };

    if !user_ok || !pass_ok {
        return VerifyResult::Denied;
    }

    // Both matched — build result.
    if !pass_hash.is_empty() {
        let token = generate_session_token();
        VerifyResult::Ok {
            token,
            upgrade_hash: None,
        }
    } else {
        let token = generate_session_token();
        VerifyResult::Ok {
            token,
            upgrade_hash: legacy_upgrade_hash,
        }
    }
}

/// Derive a deterministic session token from credentials using HMAC-SHA256.
///
/// This keeps the same token-derivation logic as before so existing sessions
/// remain valid across the migration.
#[deprecated(note = "Use generate_session_token() for random tokens with expiration")]
pub fn derive_session_token(username: &str, password: &str) -> String {
    use hmac::{Hmac, Mac};
    use sha2::Sha256;
    let mut mac =
        Hmac::<Sha256>::new_from_slice(password.as_bytes()).expect("HMAC accepts any key size");
    mac.update(username.as_bytes());
    mac.update(b"librefang-dashboard-session");
    mac.finalize()
        .into_bytes()
        .iter()
        .map(|b| format!("{b:02x}"))
        .collect()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_hash_and_verify() {
        let hash = hash_password("hunter2").unwrap();
        assert!(hash.starts_with("$argon2id$"));
        assert!(verify_password("hunter2", &hash));
        assert!(!verify_password("wrong", &hash));
    }

    #[test]
    fn test_different_passwords_produce_different_hashes() {
        let h1 = hash_password("password1").unwrap();
        let h2 = hash_password("password2").unwrap();
        assert_ne!(h1, h2);
    }

    #[test]
    fn test_same_password_produces_different_salts() {
        let h1 = hash_password("same").unwrap();
        let h2 = hash_password("same").unwrap();
        // Different salts -> different hashes, but both verify.
        assert_ne!(h1, h2);
        assert!(verify_password("same", &h1));
        assert!(verify_password("same", &h2));
    }

    #[test]
    fn test_verify_rejects_invalid_hash_format() {
        assert!(!verify_password("pass", "not-a-valid-phc-string"));
        assert!(!verify_password("pass", ""));
    }

    #[test]
    fn device_token_hash_round_trip() {
        let token = "a".repeat(64); // 32-byte hex, like the real format
        let hash = hash_device_token(&token);
        assert!(hash.starts_with("$sha256$"));
        assert!(verify_password(&token, &hash));
        assert!(!verify_password("wrong", &hash));
    }

    #[test]
    fn device_token_hash_is_deterministic() {
        // Unlike Argon2 (salted), the device-token hash is unsalted —
        // the bearer's own entropy is the secret. Two hashes of the
        // same token must therefore be byte-identical, otherwise a
        // restart would silently invalidate every paired device.
        let token = "abcd1234";
        assert_eq!(hash_device_token(token), hash_device_token(token));
    }

    #[test]
    fn verify_password_rejects_truncated_sha256_hash() {
        let token = "abcd1234";
        let mut hash = hash_device_token(token);
        hash.truncate(hash.len() - 4); // chop off last 4 hex chars
        assert!(!verify_password(token, &hash));
    }

    #[test]
    fn verify_password_rejects_sha256_hash_with_invalid_hex() {
        let bogus = "$sha256$not-actually-hex-zzz";
        assert!(!verify_password("anything", bogus));
    }

    #[test]
    fn verify_password_dispatches_by_prefix() {
        // Argon2 PHC strings start with `$argon2id$`, our SHA-256 hashes
        // start with `$sha256$`. Each must verify against its own
        // hashing function and reject a token hashed with the other.
        let token = "shared-token-string";
        let argon = hash_password(token).unwrap();
        let sha = hash_device_token(token);
        assert!(verify_password(token, &argon));
        assert!(verify_password(token, &sha));
        // Cross-format verification — same plaintext, but the stored
        // hash must dispatch to the correct algorithm and only that
        // algorithm. Both paths return true here because the plaintext
        // matches; the test ensures both prefixes are recognised.
        assert_ne!(argon, sha, "the two formats must produce distinct strings");
    }

    #[test]
    fn generated_bearer_token_is_256_bits_of_hex_and_hashes_cheaply() {
        let a = generate_bearer_token();
        let b = generate_bearer_token();
        assert_eq!(a.len(), 64, "32 bytes hex-encoded");
        assert!(a.chars().all(|c| c.is_ascii_hexdigit()));
        assert_ne!(a, b, "each call must draw fresh CSPRNG bytes");
        // The whole point: a generated key is high-entropy, so its verifier is
        // the cheap `$sha256$` form and round-trips.
        let hash = hash_device_token(&a);
        assert!(is_cheap_to_verify(&hash));
        assert!(verify_password(&a, &hash));
        assert!(!verify_password(&b, &hash));
    }

    #[test]
    fn is_cheap_to_verify_tracks_the_prefix_verify_password_dispatches_on() {
        // The whole point of the helper is that a caller can decide
        // spawn_blocking-or-not without duplicating the prefix knowledge, so
        // pin it against both real producers rather than against literals.
        assert!(is_cheap_to_verify(&hash_device_token("some-bearer")));
        assert!(!is_cheap_to_verify(
            &hash_password("some-password").unwrap()
        ));
    }

    #[test]
    fn is_cheap_to_verify_treats_garbage_as_expensive() {
        // Fail safe: an unrecognised or empty hash must not be reported cheap,
        // or a hand-edited config.toml value could put an Argon2-class cost on
        // the async runtime. `verify_password` rejects both regardless.
        assert!(!is_cheap_to_verify(""));
        assert!(!is_cheap_to_verify("not-a-phc-string"));
        assert!(!verify_password("anything", "not-a-phc-string"));
    }

    #[test]
    #[allow(deprecated)]
    fn test_derive_session_token_is_deterministic() {
        let t1 = derive_session_token("admin", "secret");
        let t2 = derive_session_token("admin", "secret");
        assert_eq!(t1, t2);
        assert!(!t1.is_empty());
    }

    #[test]
    #[allow(deprecated)]
    fn test_derive_session_token_differs_for_different_creds() {
        let t1 = derive_session_token("admin", "pass1");
        let t2 = derive_session_token("admin", "pass2");
        assert_ne!(t1, t2);
    }

    #[test]
    fn test_generate_session_token_is_random() {
        let t1 = generate_session_token();
        let t2 = generate_session_token();
        assert_ne!(t1.token, t2.token);
        assert_eq!(t1.token.len(), 64); // 32 bytes = 64 hex chars
        assert!(t1.created_at > 0);
    }

    #[test]
    fn test_session_token_not_expired() {
        let token = generate_session_token();
        assert!(!is_token_expired(&token, DEFAULT_SESSION_TTL_SECS));
    }

    #[test]
    fn test_session_token_expired() {
        let token = SessionToken {
            token: "deadbeef".to_string(),
            created_at: 0, // epoch = long ago
            user_name: None,
            user_role: None,
        };
        assert!(is_token_expired(&token, DEFAULT_SESSION_TTL_SECS));
    }

    #[test]
    fn test_session_token_zero_ttl_expires_immediately() {
        let token = generate_session_token();
        // A TTL of 0 means the token expires the instant it's created.
        assert!(is_token_expired(&token, 0));
    }

    #[test]
    fn test_verify_dashboard_argon2id_path() {
        let hash = hash_password("mypass").unwrap();
        match verify_dashboard_password("admin", "mypass", "admin", "", &hash) {
            VerifyResult::Ok {
                upgrade_hash,
                token,
            } => {
                assert!(upgrade_hash.is_none()); // Already using Argon2id
                assert!(!token.token.is_empty());
                assert_eq!(token.token.len(), 64);
                assert!(!is_token_expired(&token, DEFAULT_SESSION_TTL_SECS));
            }
            VerifyResult::Denied => panic!("should have succeeded"),
        }
    }

    #[test]
    fn test_verify_dashboard_argon2id_produces_unique_tokens() {
        let hash = hash_password("mypass").unwrap();
        let t1 = match verify_dashboard_password("admin", "mypass", "admin", "", &hash) {
            VerifyResult::Ok { token, .. } => token,
            VerifyResult::Denied => panic!("should have succeeded"),
        };
        let t2 = match verify_dashboard_password("admin", "mypass", "admin", "", &hash) {
            VerifyResult::Ok { token, .. } => token,
            VerifyResult::Denied => panic!("should have succeeded"),
        };
        // Each login produces a unique random token.
        assert_ne!(t1.token, t2.token);
    }

    #[test]
    fn test_verify_dashboard_argon2id_wrong_password() {
        let hash = hash_password("mypass").unwrap();
        assert!(matches!(
            verify_dashboard_password("admin", "wrong", "admin", "", &hash),
            VerifyResult::Denied
        ));
    }

    #[test]
    fn test_verify_dashboard_argon2id_wrong_username() {
        let hash = hash_password("mypass").unwrap();
        assert!(matches!(
            verify_dashboard_password("wrong", "mypass", "admin", "", &hash),
            VerifyResult::Denied
        ));
    }

    #[test]
    fn test_verify_dashboard_legacy_plaintext_fallback() {
        match verify_dashboard_password("admin", "secret", "admin", "secret", "") {
            VerifyResult::Ok {
                upgrade_hash,
                token,
            } => {
                // Should offer an upgrade hash
                assert!(upgrade_hash.is_some());
                let uh = upgrade_hash.unwrap();
                assert!(uh.starts_with("$argon2id$"));
                // The upgrade hash should verify against the password
                assert!(verify_password("secret", &uh));
                assert!(!token.token.is_empty());
                assert_eq!(token.token.len(), 64);
            }
            VerifyResult::Denied => panic!("should have succeeded with legacy plaintext"),
        }
    }

    #[test]
    fn test_verify_dashboard_legacy_wrong_password() {
        assert!(matches!(
            verify_dashboard_password("admin", "wrong", "admin", "secret", ""),
            VerifyResult::Denied
        ));
    }

    #[test]
    fn legacy_wrong_password_still_performs_argon2_work() {
        let (matches, dummy_hash) = verify_legacy_password("wrong", "secret");

        assert!(!matches);
        assert!(dummy_hash.is_some());
    }

    #[test]
    fn test_verify_dashboard_no_credentials() {
        assert!(matches!(
            verify_dashboard_password("admin", "pass", "admin", "", ""),
            VerifyResult::Denied
        ));
    }

    #[test]
    #[allow(deprecated)]
    fn test_session_token_matches_legacy_derivation() {
        // Verify our token derivation matches the old HMAC-SHA256 approach
        // so existing sessions are not invalidated.
        use hmac::{Hmac, Mac};
        use sha2::Sha256;
        let mut mac = Hmac::<Sha256>::new_from_slice(b"pass").expect("HMAC key");
        mac.update(b"user");
        mac.update(b"librefang-dashboard-session");
        let expected: String = mac
            .finalize()
            .into_bytes()
            .iter()
            .map(|b| format!("{b:02x}"))
            .collect();

        assert_eq!(derive_session_token("user", "pass"), expected);
    }

    #[test]
    #[allow(deprecated)]
    fn test_dashboard_session_token_uses_hash_when_available() {
        let token =
            derive_dashboard_session_token("admin", "legacy-pass", "stored-hash").expect("token");
        assert_eq!(token, derive_session_token("admin", "stored-hash"));
    }

    /// Verify that wrong-username and wrong-password both return `Denied`
    /// and that the password verification path is always exercised
    /// (i.e., no early return on username mismatch that would leak timing).
    #[test]
    fn test_timing_constant_on_wrong_username() {
        let hash = hash_password("correct-pass").unwrap();

        // Wrong username, correct password — must be Denied.
        assert!(matches!(
            verify_dashboard_password("wrong-user", "correct-pass", "admin", "", &hash),
            VerifyResult::Denied
        ));

        // Correct username, wrong password — must be Denied.
        assert!(matches!(
            verify_dashboard_password("admin", "wrong-pass", "admin", "", &hash),
            VerifyResult::Denied
        ));

        // Wrong username, wrong password — must be Denied.
        assert!(matches!(
            verify_dashboard_password("wrong-user", "wrong-pass", "admin", "", &hash),
            VerifyResult::Denied
        ));

        // Legacy plaintext path: wrong username, correct password — must be Denied.
        assert!(matches!(
            verify_dashboard_password("wrong-user", "secret", "admin", "secret", ""),
            VerifyResult::Denied
        ));

        // No credentials path: wrong username — must be Denied (and still runs
        // a dummy hash rather than returning instantly).
        assert!(matches!(
            verify_dashboard_password("wrong-user", "pass", "admin", "", ""),
            VerifyResult::Denied
        ));
    }

    #[test]
    fn test_session_token_serialization_roundtrip() {
        let token = generate_session_token();
        let json = serde_json::to_string(&token).unwrap();
        let deserialized: SessionToken = serde_json::from_str(&json).unwrap();
        assert_eq!(token.token, deserialized.token);
        assert_eq!(token.created_at, deserialized.created_at);
    }
}
